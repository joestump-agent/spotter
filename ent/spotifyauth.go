// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"spotter/ent/spotifyauth"
	"spotter/ent/user"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// SpotifyAuth is the model entity for the SpotifyAuth schema.
type SpotifyAuth struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// AccessToken holds the value of the "access_token" field.
	AccessToken string `json:"access_token,omitempty"`
	// RefreshToken holds the value of the "refresh_token" field.
	RefreshToken string `json:"refresh_token,omitempty"`
	// Expiry holds the value of the "expiry" field.
	Expiry time.Time `json:"expiry,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SpotifyAuthQuery when eager-loading is set.
	Edges             SpotifyAuthEdges `json:"edges"`
	user_spotify_auth *int
	selectValues      sql.SelectValues
}

// SpotifyAuthEdges holds the relations/edges for other nodes in the graph.
type SpotifyAuthEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e SpotifyAuthEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SpotifyAuth) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case spotifyauth.FieldID:
			values[i] = new(sql.NullInt64)
		case spotifyauth.FieldAccessToken, spotifyauth.FieldRefreshToken:
			values[i] = new(sql.NullString)
		case spotifyauth.FieldExpiry:
			values[i] = new(sql.NullTime)
		case spotifyauth.ForeignKeys[0]: // user_spotify_auth
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SpotifyAuth fields.
func (_m *SpotifyAuth) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case spotifyauth.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case spotifyauth.FieldAccessToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field access_token", values[i])
			} else if value.Valid {
				_m.AccessToken = value.String
			}
		case spotifyauth.FieldRefreshToken:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field refresh_token", values[i])
			} else if value.Valid {
				_m.RefreshToken = value.String
			}
		case spotifyauth.FieldExpiry:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field expiry", values[i])
			} else if value.Valid {
				_m.Expiry = value.Time
			}
		case spotifyauth.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_spotify_auth", value)
			} else if value.Valid {
				_m.user_spotify_auth = new(int)
				*_m.user_spotify_auth = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SpotifyAuth.
// This includes values selected through modifiers, order, etc.
func (_m *SpotifyAuth) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the SpotifyAuth entity.
func (_m *SpotifyAuth) QueryUser() *UserQuery {
	return NewSpotifyAuthClient(_m.config).QueryUser(_m)
}

// Update returns a builder for updating this SpotifyAuth.
// Note that you need to call SpotifyAuth.Unwrap() before calling this method if this SpotifyAuth
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *SpotifyAuth) Update() *SpotifyAuthUpdateOne {
	return NewSpotifyAuthClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the SpotifyAuth entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *SpotifyAuth) Unwrap() *SpotifyAuth {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: SpotifyAuth is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *SpotifyAuth) String() string {
	var builder strings.Builder
	builder.WriteString("SpotifyAuth(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("access_token=")
	builder.WriteString(_m.AccessToken)
	builder.WriteString(", ")
	builder.WriteString("refresh_token=")
	builder.WriteString(_m.RefreshToken)
	builder.WriteString(", ")
	builder.WriteString("expiry=")
	builder.WriteString(_m.Expiry.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SpotifyAuths is a parsable slice of SpotifyAuth.
type SpotifyAuths []*SpotifyAuth
