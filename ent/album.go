// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"spotter/ent/album"
	"spotter/ent/artist"
	"spotter/ent/user"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Album is the model entity for the Album schema.
type Album struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Name used for sorting, e.g., 'White Album, The'
	SortName string `json:"sort_name,omitempty"`
	// MusicBrainz release group ID
	MusicbrainzID string `json:"musicbrainz_id,omitempty"`
	// Spotify album ID
	SpotifyID string `json:"spotify_id,omitempty"`
	// Release date in ISO format (YYYY, YYYY-MM, or YYYY-MM-DD)
	ReleaseDate string `json:"release_date,omitempty"`
	// Release year extracted from release_date
	Year int `json:"year,omitempty"`
	// Primary genre of the album
	Genre string `json:"genre,omitempty"`
	// Tags/genres from various sources
	Tags []string `json:"tags,omitempty"`
	// Popularity score (0-100, from Spotify)
	Popularity int `json:"popularity,omitempty"`
	// Total number of tracks on the album
	TotalTracks int `json:"total_tracks,omitempty"`
	// Type: album, single, compilation, ep
	AlbumType string `json:"album_type,omitempty"`
	// Record label
	Label string `json:"label,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// When metadata was last enriched
	LastEnrichedAt time.Time `json:"last_enriched_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AlbumQuery when eager-loading is set.
	Edges         AlbumEdges `json:"edges"`
	artist_albums *int
	user_albums   *int
	selectValues  sql.SelectValues
}

// AlbumEdges holds the relations/edges for other nodes in the graph.
type AlbumEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Artist holds the value of the artist edge.
	Artist *Artist `json:"artist,omitempty"`
	// Tracks holds the value of the tracks edge.
	Tracks []*Track `json:"tracks,omitempty"`
	// Images holds the value of the images edge.
	Images []*AlbumImage `json:"images,omitempty"`
	// Listens holds the value of the listens edge.
	Listens []*Listen `json:"listens,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [5]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e AlbumEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// ArtistOrErr returns the Artist value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e AlbumEdges) ArtistOrErr() (*Artist, error) {
	if e.Artist != nil {
		return e.Artist, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: artist.Label}
	}
	return nil, &NotLoadedError{edge: "artist"}
}

// TracksOrErr returns the Tracks value or an error if the edge
// was not loaded in eager-loading.
func (e AlbumEdges) TracksOrErr() ([]*Track, error) {
	if e.loadedTypes[2] {
		return e.Tracks, nil
	}
	return nil, &NotLoadedError{edge: "tracks"}
}

// ImagesOrErr returns the Images value or an error if the edge
// was not loaded in eager-loading.
func (e AlbumEdges) ImagesOrErr() ([]*AlbumImage, error) {
	if e.loadedTypes[3] {
		return e.Images, nil
	}
	return nil, &NotLoadedError{edge: "images"}
}

// ListensOrErr returns the Listens value or an error if the edge
// was not loaded in eager-loading.
func (e AlbumEdges) ListensOrErr() ([]*Listen, error) {
	if e.loadedTypes[4] {
		return e.Listens, nil
	}
	return nil, &NotLoadedError{edge: "listens"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Album) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case album.FieldTags:
			values[i] = new([]byte)
		case album.FieldID, album.FieldYear, album.FieldPopularity, album.FieldTotalTracks:
			values[i] = new(sql.NullInt64)
		case album.FieldName, album.FieldSortName, album.FieldMusicbrainzID, album.FieldSpotifyID, album.FieldReleaseDate, album.FieldGenre, album.FieldAlbumType, album.FieldLabel:
			values[i] = new(sql.NullString)
		case album.FieldCreatedAt, album.FieldUpdatedAt, album.FieldLastEnrichedAt:
			values[i] = new(sql.NullTime)
		case album.ForeignKeys[0]: // artist_albums
			values[i] = new(sql.NullInt64)
		case album.ForeignKeys[1]: // user_albums
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Album fields.
func (_m *Album) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case album.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case album.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case album.FieldSortName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field sort_name", values[i])
			} else if value.Valid {
				_m.SortName = value.String
			}
		case album.FieldMusicbrainzID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field musicbrainz_id", values[i])
			} else if value.Valid {
				_m.MusicbrainzID = value.String
			}
		case album.FieldSpotifyID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field spotify_id", values[i])
			} else if value.Valid {
				_m.SpotifyID = value.String
			}
		case album.FieldReleaseDate:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field release_date", values[i])
			} else if value.Valid {
				_m.ReleaseDate = value.String
			}
		case album.FieldYear:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field year", values[i])
			} else if value.Valid {
				_m.Year = int(value.Int64)
			}
		case album.FieldGenre:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field genre", values[i])
			} else if value.Valid {
				_m.Genre = value.String
			}
		case album.FieldTags:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field tags", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Tags); err != nil {
					return fmt.Errorf("unmarshal field tags: %w", err)
				}
			}
		case album.FieldPopularity:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field popularity", values[i])
			} else if value.Valid {
				_m.Popularity = int(value.Int64)
			}
		case album.FieldTotalTracks:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field total_tracks", values[i])
			} else if value.Valid {
				_m.TotalTracks = int(value.Int64)
			}
		case album.FieldAlbumType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field album_type", values[i])
			} else if value.Valid {
				_m.AlbumType = value.String
			}
		case album.FieldLabel:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field label", values[i])
			} else if value.Valid {
				_m.Label = value.String
			}
		case album.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case album.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case album.FieldLastEnrichedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_enriched_at", values[i])
			} else if value.Valid {
				_m.LastEnrichedAt = value.Time
			}
		case album.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field artist_albums", value)
			} else if value.Valid {
				_m.artist_albums = new(int)
				*_m.artist_albums = int(value.Int64)
			}
		case album.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_albums", value)
			} else if value.Valid {
				_m.user_albums = new(int)
				*_m.user_albums = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Album.
// This includes values selected through modifiers, order, etc.
func (_m *Album) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the Album entity.
func (_m *Album) QueryUser() *UserQuery {
	return NewAlbumClient(_m.config).QueryUser(_m)
}

// QueryArtist queries the "artist" edge of the Album entity.
func (_m *Album) QueryArtist() *ArtistQuery {
	return NewAlbumClient(_m.config).QueryArtist(_m)
}

// QueryTracks queries the "tracks" edge of the Album entity.
func (_m *Album) QueryTracks() *TrackQuery {
	return NewAlbumClient(_m.config).QueryTracks(_m)
}

// QueryImages queries the "images" edge of the Album entity.
func (_m *Album) QueryImages() *AlbumImageQuery {
	return NewAlbumClient(_m.config).QueryImages(_m)
}

// QueryListens queries the "listens" edge of the Album entity.
func (_m *Album) QueryListens() *ListenQuery {
	return NewAlbumClient(_m.config).QueryListens(_m)
}

// Update returns a builder for updating this Album.
// Note that you need to call Album.Unwrap() before calling this method if this Album
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Album) Update() *AlbumUpdateOne {
	return NewAlbumClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Album entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Album) Unwrap() *Album {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Album is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Album) String() string {
	var builder strings.Builder
	builder.WriteString("Album(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("sort_name=")
	builder.WriteString(_m.SortName)
	builder.WriteString(", ")
	builder.WriteString("musicbrainz_id=")
	builder.WriteString(_m.MusicbrainzID)
	builder.WriteString(", ")
	builder.WriteString("spotify_id=")
	builder.WriteString(_m.SpotifyID)
	builder.WriteString(", ")
	builder.WriteString("release_date=")
	builder.WriteString(_m.ReleaseDate)
	builder.WriteString(", ")
	builder.WriteString("year=")
	builder.WriteString(fmt.Sprintf("%v", _m.Year))
	builder.WriteString(", ")
	builder.WriteString("genre=")
	builder.WriteString(_m.Genre)
	builder.WriteString(", ")
	builder.WriteString("tags=")
	builder.WriteString(fmt.Sprintf("%v", _m.Tags))
	builder.WriteString(", ")
	builder.WriteString("popularity=")
	builder.WriteString(fmt.Sprintf("%v", _m.Popularity))
	builder.WriteString(", ")
	builder.WriteString("total_tracks=")
	builder.WriteString(fmt.Sprintf("%v", _m.TotalTracks))
	builder.WriteString(", ")
	builder.WriteString("album_type=")
	builder.WriteString(_m.AlbumType)
	builder.WriteString(", ")
	builder.WriteString("label=")
	builder.WriteString(_m.Label)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("last_enriched_at=")
	builder.WriteString(_m.LastEnrichedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Albums is a parsable slice of Album.
type Albums []*Album
