// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"spotter/ent/lastfmauth"
	"spotter/ent/listen"
	"spotter/ent/navidromeauth"
	"spotter/ent/predicate"
	"spotter/ent/spotifyauth"
	"spotter/ent/user"
	"sync"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

const (
	// Operation types.
	OpCreate    = ent.OpCreate
	OpDelete    = ent.OpDelete
	OpDeleteOne = ent.OpDeleteOne
	OpUpdate    = ent.OpUpdate
	OpUpdateOne = ent.OpUpdateOne

	// Node types.
	TypeLastFMAuth    = "LastFMAuth"
	TypeListen        = "Listen"
	TypeNavidromeAuth = "NavidromeAuth"
	TypeSpotifyAuth   = "SpotifyAuth"
	TypeUser          = "User"
)

// LastFMAuthMutation represents an operation that mutates the LastFMAuth nodes in the graph.
type LastFMAuthMutation struct {
	config
	op            Op
	typ           string
	id            *int
	session_key   *string
	username      *string
	clearedFields map[string]struct{}
	user          *int
	cleareduser   bool
	done          bool
	oldValue      func(context.Context) (*LastFMAuth, error)
	predicates    []predicate.LastFMAuth
}

var _ ent.Mutation = (*LastFMAuthMutation)(nil)

// lastfmauthOption allows management of the mutation configuration using functional options.
type lastfmauthOption func(*LastFMAuthMutation)

// newLastFMAuthMutation creates new mutation for the LastFMAuth entity.
func newLastFMAuthMutation(c config, op Op, opts ...lastfmauthOption) *LastFMAuthMutation {
	m := &LastFMAuthMutation{
		config:        c,
		op:            op,
		typ:           TypeLastFMAuth,
		clearedFields: make(map[string]struct{}),
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// withLastFMAuthID sets the ID field of the mutation.
func withLastFMAuthID(id int) lastfmauthOption {
	return func(m *LastFMAuthMutation) {
		var (
			err   error
			once  sync.Once
			value *LastFMAuth
		)
		m.oldValue = func(ctx context.Context) (*LastFMAuth, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().LastFMAuth.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withLastFMAuth sets the old LastFMAuth of the mutation.
func withLastFMAuth(node *LastFMAuth) lastfmauthOption {
	return func(m *LastFMAuthMutation) {
		m.oldValue = func(context.Context) (*LastFMAuth, error) {
			return node, nil
		}
		m.id = &node.ID
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m LastFMAuthMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m LastFMAuthMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// ID returns the ID value in the mutation. Note that the ID is only available
// if it was provided to the builder or after it was returned from the database.
func (m *LastFMAuthMutation) ID() (id int, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// IDs queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *LastFMAuthMutation) IDs(ctx context.Context) ([]int, error) {
	switch {
	case m.op.Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.ID()
		if exists {
			return []int{id}, nil
		}
		fallthrough
	case m.op.Is(OpUpdate | OpDelete):
		return m.Client().LastFMAuth.Query().Where(m.predicates...).IDs(ctx)
	default:
		return nil, fmt.Errorf("IDs is not allowed on %s operations", m.op)
	}
}

// SetSessionKey sets the "session_key" field.
func (m *LastFMAuthMutation) SetSessionKey(s string) {
	m.session_key = &s
}

// SessionKey returns the value of the "session_key" field in the mutation.
func (m *LastFMAuthMutation) SessionKey() (r string, exists bool) {
	v := m.session_key
	if v == nil {
		return
	}
	return *v, true
}

// OldSessionKey returns the old "session_key" field's value of the LastFMAuth entity.
// If the LastFMAuth object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *LastFMAuthMutation) OldSessionKey(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldSessionKey is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldSessionKey requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldSessionKey: %w", err)
	}
	return oldValue.SessionKey, nil
}

// ResetSessionKey resets all changes to the "session_key" field.
func (m *LastFMAuthMutation) ResetSessionKey() {
	m.session_key = nil
}

// SetUsername sets the "username" field.
func (m *LastFMAuthMutation) SetUsername(s string) {
	m.username = &s
}

// Username returns the value of the "username" field in the mutation.
func (m *LastFMAuthMutation) Username() (r string, exists bool) {
	v := m.username
	if v == nil {
		return
	}
	return *v, true
}

// OldUsername returns the old "username" field's value of the LastFMAuth entity.
// If the LastFMAuth object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *LastFMAuthMutation) OldUsername(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldUsername is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldUsername requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldUsername: %w", err)
	}
	return oldValue.Username, nil
}

// ResetUsername resets all changes to the "username" field.
func (m *LastFMAuthMutation) ResetUsername() {
	m.username = nil
}

// SetUserID sets the "user" edge to the User entity by id.
func (m *LastFMAuthMutation) SetUserID(id int) {
	m.user = &id
}

// ClearUser clears the "user" edge to the User entity.
func (m *LastFMAuthMutation) ClearUser() {
	m.cleareduser = true
}

// UserCleared reports if the "user" edge to the User entity was cleared.
func (m *LastFMAuthMutation) UserCleared() bool {
	return m.cleareduser
}

// UserID returns the "user" edge ID in the mutation.
func (m *LastFMAuthMutation) UserID() (id int, exists bool) {
	if m.user != nil {
		return *m.user, true
	}
	return
}

// UserIDs returns the "user" edge IDs in the mutation.
// Note that IDs always returns len(IDs) <= 1 for unique edges, and you should use
// UserID instead. It exists only for internal usage by the builders.
func (m *LastFMAuthMutation) UserIDs() (ids []int) {
	if id := m.user; id != nil {
		ids = append(ids, *id)
	}
	return
}

// ResetUser resets all changes to the "user" edge.
func (m *LastFMAuthMutation) ResetUser() {
	m.user = nil
	m.cleareduser = false
}

// Where appends a list predicates to the LastFMAuthMutation builder.
func (m *LastFMAuthMutation) Where(ps ...predicate.LastFMAuth) {
	m.predicates = append(m.predicates, ps...)
}

// WhereP appends storage-level predicates to the LastFMAuthMutation builder. Using this method,
// users can use type-assertion to append predicates that do not depend on any generated package.
func (m *LastFMAuthMutation) WhereP(ps ...func(*sql.Selector)) {
	p := make([]predicate.LastFMAuth, len(ps))
	for i := range ps {
		p[i] = ps[i]
	}
	m.Where(p...)
}

// Op returns the operation name.
func (m *LastFMAuthMutation) Op() Op {
	return m.op
}

// SetOp allows setting the mutation operation.
func (m *LastFMAuthMutation) SetOp(op Op) {
	m.op = op
}

// Type returns the node type of this mutation (LastFMAuth).
func (m *LastFMAuthMutation) Type() string {
	return m.typ
}

// Fields returns all fields that were changed during this mutation. Note that in
// order to get all numeric fields that were incremented/decremented, call
// AddedFields().
func (m *LastFMAuthMutation) Fields() []string {
	fields := make([]string, 0, 2)
	if m.session_key != nil {
		fields = append(fields, lastfmauth.FieldSessionKey)
	}
	if m.username != nil {
		fields = append(fields, lastfmauth.FieldUsername)
	}
	return fields
}

// Field returns the value of a field with the given name. The second boolean
// return value indicates that this field was not set, or was not defined in the
// schema.
func (m *LastFMAuthMutation) Field(name string) (ent.Value, bool) {
	switch name {
	case lastfmauth.FieldSessionKey:
		return m.SessionKey()
	case lastfmauth.FieldUsername:
		return m.Username()
	}
	return nil, false
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *LastFMAuthMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case lastfmauth.FieldSessionKey:
		return m.OldSessionKey(ctx)
	case lastfmauth.FieldUsername:
		return m.OldUsername(ctx)
	}
	return nil, fmt.Errorf("unknown LastFMAuth field %s", name)
}

// SetField sets the value of a field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *LastFMAuthMutation) SetField(name string, value ent.Value) error {
	switch name {
	case lastfmauth.FieldSessionKey:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetSessionKey(v)
		return nil
	case lastfmauth.FieldUsername:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetUsername(v)
		return nil
	}
	return fmt.Errorf("unknown LastFMAuth field %s", name)
}

// AddedFields returns all numeric fields that were incremented/decremented during
// this mutation.
func (m *LastFMAuthMutation) AddedFields() []string {
	return nil
}

// AddedField returns the numeric value that was incremented/decremented on a field
// with the given name. The second boolean return value indicates that this field
// was not set, or was not defined in the schema.
func (m *LastFMAuthMutation) AddedField(name string) (ent.Value, bool) {
	return nil, false
}

// AddField adds the value to the field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *LastFMAuthMutation) AddField(name string, value ent.Value) error {
	switch name {
	}
	return fmt.Errorf("unknown LastFMAuth numeric field %s", name)
}

// ClearedFields returns all nullable fields that were cleared during this
// mutation.
func (m *LastFMAuthMutation) ClearedFields() []string {
	return nil
}

// FieldCleared returns a boolean indicating if a field with the given name was
// cleared in this mutation.
func (m *LastFMAuthMutation) FieldCleared(name string) bool {
	_, ok := m.clearedFields[name]
	return ok
}

// ClearField clears the value of the field with the given name. It returns an
// error if the field is not defined in the schema.
func (m *LastFMAuthMutation) ClearField(name string) error {
	return fmt.Errorf("unknown LastFMAuth nullable field %s", name)
}

// ResetField resets all changes in the mutation for the field with the given name.
// It returns an error if the field is not defined in the schema.
func (m *LastFMAuthMutation) ResetField(name string) error {
	switch name {
	case lastfmauth.FieldSessionKey:
		m.ResetSessionKey()
		return nil
	case lastfmauth.FieldUsername:
		m.ResetUsername()
		return nil
	}
	return fmt.Errorf("unknown LastFMAuth field %s", name)
}

// AddedEdges returns all edge names that were set/added in this mutation.
func (m *LastFMAuthMutation) AddedEdges() []string {
	edges := make([]string, 0, 1)
	if m.user != nil {
		edges = append(edges, lastfmauth.EdgeUser)
	}
	return edges
}

// AddedIDs returns all IDs (to other nodes) that were added for the given edge
// name in this mutation.
func (m *LastFMAuthMutation) AddedIDs(name string) []ent.Value {
	switch name {
	case lastfmauth.EdgeUser:
		if id := m.user; id != nil {
			return []ent.Value{*id}
		}
	}
	return nil
}

// RemovedEdges returns all edge names that were removed in this mutation.
func (m *LastFMAuthMutation) RemovedEdges() []string {
	edges := make([]string, 0, 1)
	return edges
}

// RemovedIDs returns all IDs (to other nodes) that were removed for the edge with
// the given name in this mutation.
func (m *LastFMAuthMutation) RemovedIDs(name string) []ent.Value {
	return nil
}

// ClearedEdges returns all edge names that were cleared in this mutation.
func (m *LastFMAuthMutation) ClearedEdges() []string {
	edges := make([]string, 0, 1)
	if m.cleareduser {
		edges = append(edges, lastfmauth.EdgeUser)
	}
	return edges
}

// EdgeCleared returns a boolean which indicates if the edge with the given name
// was cleared in this mutation.
func (m *LastFMAuthMutation) EdgeCleared(name string) bool {
	switch name {
	case lastfmauth.EdgeUser:
		return m.cleareduser
	}
	return false
}

// ClearEdge clears the value of the edge with the given name. It returns an error
// if that edge is not defined in the schema.
func (m *LastFMAuthMutation) ClearEdge(name string) error {
	switch name {
	case lastfmauth.EdgeUser:
		m.ClearUser()
		return nil
	}
	return fmt.Errorf("unknown LastFMAuth unique edge %s", name)
}

// ResetEdge resets all changes to the edge with the given name in this mutation.
// It returns an error if the edge is not defined in the schema.
func (m *LastFMAuthMutation) ResetEdge(name string) error {
	switch name {
	case lastfmauth.EdgeUser:
		m.ResetUser()
		return nil
	}
	return fmt.Errorf("unknown LastFMAuth edge %s", name)
}

// ListenMutation represents an operation that mutates the Listen nodes in the graph.
type ListenMutation struct {
	config
	op            Op
	typ           string
	id            *int
	track_name    *string
	artist_name   *string
	album_name    *string
	source        *string
	played_at     *time.Time
	url           *string
	clearedFields map[string]struct{}
	user          *int
	cleareduser   bool
	done          bool
	oldValue      func(context.Context) (*Listen, error)
	predicates    []predicate.Listen
}

var _ ent.Mutation = (*ListenMutation)(nil)

// listenOption allows management of the mutation configuration using functional options.
type listenOption func(*ListenMutation)

// newListenMutation creates new mutation for the Listen entity.
func newListenMutation(c config, op Op, opts ...listenOption) *ListenMutation {
	m := &ListenMutation{
		config:        c,
		op:            op,
		typ:           TypeListen,
		clearedFields: make(map[string]struct{}),
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// withListenID sets the ID field of the mutation.
func withListenID(id int) listenOption {
	return func(m *ListenMutation) {
		var (
			err   error
			once  sync.Once
			value *Listen
		)
		m.oldValue = func(ctx context.Context) (*Listen, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().Listen.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withListen sets the old Listen of the mutation.
func withListen(node *Listen) listenOption {
	return func(m *ListenMutation) {
		m.oldValue = func(context.Context) (*Listen, error) {
			return node, nil
		}
		m.id = &node.ID
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m ListenMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m ListenMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// ID returns the ID value in the mutation. Note that the ID is only available
// if it was provided to the builder or after it was returned from the database.
func (m *ListenMutation) ID() (id int, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// IDs queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *ListenMutation) IDs(ctx context.Context) ([]int, error) {
	switch {
	case m.op.Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.ID()
		if exists {
			return []int{id}, nil
		}
		fallthrough
	case m.op.Is(OpUpdate | OpDelete):
		return m.Client().Listen.Query().Where(m.predicates...).IDs(ctx)
	default:
		return nil, fmt.Errorf("IDs is not allowed on %s operations", m.op)
	}
}

// SetTrackName sets the "track_name" field.
func (m *ListenMutation) SetTrackName(s string) {
	m.track_name = &s
}

// TrackName returns the value of the "track_name" field in the mutation.
func (m *ListenMutation) TrackName() (r string, exists bool) {
	v := m.track_name
	if v == nil {
		return
	}
	return *v, true
}

// OldTrackName returns the old "track_name" field's value of the Listen entity.
// If the Listen object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ListenMutation) OldTrackName(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldTrackName is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldTrackName requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldTrackName: %w", err)
	}
	return oldValue.TrackName, nil
}

// ResetTrackName resets all changes to the "track_name" field.
func (m *ListenMutation) ResetTrackName() {
	m.track_name = nil
}

// SetArtistName sets the "artist_name" field.
func (m *ListenMutation) SetArtistName(s string) {
	m.artist_name = &s
}

// ArtistName returns the value of the "artist_name" field in the mutation.
func (m *ListenMutation) ArtistName() (r string, exists bool) {
	v := m.artist_name
	if v == nil {
		return
	}
	return *v, true
}

// OldArtistName returns the old "artist_name" field's value of the Listen entity.
// If the Listen object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ListenMutation) OldArtistName(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldArtistName is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldArtistName requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldArtistName: %w", err)
	}
	return oldValue.ArtistName, nil
}

// ResetArtistName resets all changes to the "artist_name" field.
func (m *ListenMutation) ResetArtistName() {
	m.artist_name = nil
}

// SetAlbumName sets the "album_name" field.
func (m *ListenMutation) SetAlbumName(s string) {
	m.album_name = &s
}

// AlbumName returns the value of the "album_name" field in the mutation.
func (m *ListenMutation) AlbumName() (r string, exists bool) {
	v := m.album_name
	if v == nil {
		return
	}
	return *v, true
}

// OldAlbumName returns the old "album_name" field's value of the Listen entity.
// If the Listen object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ListenMutation) OldAlbumName(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldAlbumName is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldAlbumName requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldAlbumName: %w", err)
	}
	return oldValue.AlbumName, nil
}

// ResetAlbumName resets all changes to the "album_name" field.
func (m *ListenMutation) ResetAlbumName() {
	m.album_name = nil
}

// SetSource sets the "source" field.
func (m *ListenMutation) SetSource(s string) {
	m.source = &s
}

// Source returns the value of the "source" field in the mutation.
func (m *ListenMutation) Source() (r string, exists bool) {
	v := m.source
	if v == nil {
		return
	}
	return *v, true
}

// OldSource returns the old "source" field's value of the Listen entity.
// If the Listen object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ListenMutation) OldSource(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldSource is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldSource requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldSource: %w", err)
	}
	return oldValue.Source, nil
}

// ResetSource resets all changes to the "source" field.
func (m *ListenMutation) ResetSource() {
	m.source = nil
}

// SetPlayedAt sets the "played_at" field.
func (m *ListenMutation) SetPlayedAt(t time.Time) {
	m.played_at = &t
}

// PlayedAt returns the value of the "played_at" field in the mutation.
func (m *ListenMutation) PlayedAt() (r time.Time, exists bool) {
	v := m.played_at
	if v == nil {
		return
	}
	return *v, true
}

// OldPlayedAt returns the old "played_at" field's value of the Listen entity.
// If the Listen object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ListenMutation) OldPlayedAt(ctx context.Context) (v time.Time, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldPlayedAt is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldPlayedAt requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldPlayedAt: %w", err)
	}
	return oldValue.PlayedAt, nil
}

// ResetPlayedAt resets all changes to the "played_at" field.
func (m *ListenMutation) ResetPlayedAt() {
	m.played_at = nil
}

// SetURL sets the "url" field.
func (m *ListenMutation) SetURL(s string) {
	m.url = &s
}

// URL returns the value of the "url" field in the mutation.
func (m *ListenMutation) URL() (r string, exists bool) {
	v := m.url
	if v == nil {
		return
	}
	return *v, true
}

// OldURL returns the old "url" field's value of the Listen entity.
// If the Listen object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *ListenMutation) OldURL(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldURL is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldURL requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldURL: %w", err)
	}
	return oldValue.URL, nil
}

// ClearURL clears the value of the "url" field.
func (m *ListenMutation) ClearURL() {
	m.url = nil
	m.clearedFields[listen.FieldURL] = struct{}{}
}

// URLCleared returns if the "url" field was cleared in this mutation.
func (m *ListenMutation) URLCleared() bool {
	_, ok := m.clearedFields[listen.FieldURL]
	return ok
}

// ResetURL resets all changes to the "url" field.
func (m *ListenMutation) ResetURL() {
	m.url = nil
	delete(m.clearedFields, listen.FieldURL)
}

// SetUserID sets the "user" edge to the User entity by id.
func (m *ListenMutation) SetUserID(id int) {
	m.user = &id
}

// ClearUser clears the "user" edge to the User entity.
func (m *ListenMutation) ClearUser() {
	m.cleareduser = true
}

// UserCleared reports if the "user" edge to the User entity was cleared.
func (m *ListenMutation) UserCleared() bool {
	return m.cleareduser
}

// UserID returns the "user" edge ID in the mutation.
func (m *ListenMutation) UserID() (id int, exists bool) {
	if m.user != nil {
		return *m.user, true
	}
	return
}

// UserIDs returns the "user" edge IDs in the mutation.
// Note that IDs always returns len(IDs) <= 1 for unique edges, and you should use
// UserID instead. It exists only for internal usage by the builders.
func (m *ListenMutation) UserIDs() (ids []int) {
	if id := m.user; id != nil {
		ids = append(ids, *id)
	}
	return
}

// ResetUser resets all changes to the "user" edge.
func (m *ListenMutation) ResetUser() {
	m.user = nil
	m.cleareduser = false
}

// Where appends a list predicates to the ListenMutation builder.
func (m *ListenMutation) Where(ps ...predicate.Listen) {
	m.predicates = append(m.predicates, ps...)
}

// WhereP appends storage-level predicates to the ListenMutation builder. Using this method,
// users can use type-assertion to append predicates that do not depend on any generated package.
func (m *ListenMutation) WhereP(ps ...func(*sql.Selector)) {
	p := make([]predicate.Listen, len(ps))
	for i := range ps {
		p[i] = ps[i]
	}
	m.Where(p...)
}

// Op returns the operation name.
func (m *ListenMutation) Op() Op {
	return m.op
}

// SetOp allows setting the mutation operation.
func (m *ListenMutation) SetOp(op Op) {
	m.op = op
}

// Type returns the node type of this mutation (Listen).
func (m *ListenMutation) Type() string {
	return m.typ
}

// Fields returns all fields that were changed during this mutation. Note that in
// order to get all numeric fields that were incremented/decremented, call
// AddedFields().
func (m *ListenMutation) Fields() []string {
	fields := make([]string, 0, 6)
	if m.track_name != nil {
		fields = append(fields, listen.FieldTrackName)
	}
	if m.artist_name != nil {
		fields = append(fields, listen.FieldArtistName)
	}
	if m.album_name != nil {
		fields = append(fields, listen.FieldAlbumName)
	}
	if m.source != nil {
		fields = append(fields, listen.FieldSource)
	}
	if m.played_at != nil {
		fields = append(fields, listen.FieldPlayedAt)
	}
	if m.url != nil {
		fields = append(fields, listen.FieldURL)
	}
	return fields
}

// Field returns the value of a field with the given name. The second boolean
// return value indicates that this field was not set, or was not defined in the
// schema.
func (m *ListenMutation) Field(name string) (ent.Value, bool) {
	switch name {
	case listen.FieldTrackName:
		return m.TrackName()
	case listen.FieldArtistName:
		return m.ArtistName()
	case listen.FieldAlbumName:
		return m.AlbumName()
	case listen.FieldSource:
		return m.Source()
	case listen.FieldPlayedAt:
		return m.PlayedAt()
	case listen.FieldURL:
		return m.URL()
	}
	return nil, false
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *ListenMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case listen.FieldTrackName:
		return m.OldTrackName(ctx)
	case listen.FieldArtistName:
		return m.OldArtistName(ctx)
	case listen.FieldAlbumName:
		return m.OldAlbumName(ctx)
	case listen.FieldSource:
		return m.OldSource(ctx)
	case listen.FieldPlayedAt:
		return m.OldPlayedAt(ctx)
	case listen.FieldURL:
		return m.OldURL(ctx)
	}
	return nil, fmt.Errorf("unknown Listen field %s", name)
}

// SetField sets the value of a field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *ListenMutation) SetField(name string, value ent.Value) error {
	switch name {
	case listen.FieldTrackName:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetTrackName(v)
		return nil
	case listen.FieldArtistName:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetArtistName(v)
		return nil
	case listen.FieldAlbumName:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetAlbumName(v)
		return nil
	case listen.FieldSource:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetSource(v)
		return nil
	case listen.FieldPlayedAt:
		v, ok := value.(time.Time)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetPlayedAt(v)
		return nil
	case listen.FieldURL:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetURL(v)
		return nil
	}
	return fmt.Errorf("unknown Listen field %s", name)
}

// AddedFields returns all numeric fields that were incremented/decremented during
// this mutation.
func (m *ListenMutation) AddedFields() []string {
	return nil
}

// AddedField returns the numeric value that was incremented/decremented on a field
// with the given name. The second boolean return value indicates that this field
// was not set, or was not defined in the schema.
func (m *ListenMutation) AddedField(name string) (ent.Value, bool) {
	return nil, false
}

// AddField adds the value to the field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *ListenMutation) AddField(name string, value ent.Value) error {
	switch name {
	}
	return fmt.Errorf("unknown Listen numeric field %s", name)
}

// ClearedFields returns all nullable fields that were cleared during this
// mutation.
func (m *ListenMutation) ClearedFields() []string {
	var fields []string
	if m.FieldCleared(listen.FieldURL) {
		fields = append(fields, listen.FieldURL)
	}
	return fields
}

// FieldCleared returns a boolean indicating if a field with the given name was
// cleared in this mutation.
func (m *ListenMutation) FieldCleared(name string) bool {
	_, ok := m.clearedFields[name]
	return ok
}

// ClearField clears the value of the field with the given name. It returns an
// error if the field is not defined in the schema.
func (m *ListenMutation) ClearField(name string) error {
	switch name {
	case listen.FieldURL:
		m.ClearURL()
		return nil
	}
	return fmt.Errorf("unknown Listen nullable field %s", name)
}

// ResetField resets all changes in the mutation for the field with the given name.
// It returns an error if the field is not defined in the schema.
func (m *ListenMutation) ResetField(name string) error {
	switch name {
	case listen.FieldTrackName:
		m.ResetTrackName()
		return nil
	case listen.FieldArtistName:
		m.ResetArtistName()
		return nil
	case listen.FieldAlbumName:
		m.ResetAlbumName()
		return nil
	case listen.FieldSource:
		m.ResetSource()
		return nil
	case listen.FieldPlayedAt:
		m.ResetPlayedAt()
		return nil
	case listen.FieldURL:
		m.ResetURL()
		return nil
	}
	return fmt.Errorf("unknown Listen field %s", name)
}

// AddedEdges returns all edge names that were set/added in this mutation.
func (m *ListenMutation) AddedEdges() []string {
	edges := make([]string, 0, 1)
	if m.user != nil {
		edges = append(edges, listen.EdgeUser)
	}
	return edges
}

// AddedIDs returns all IDs (to other nodes) that were added for the given edge
// name in this mutation.
func (m *ListenMutation) AddedIDs(name string) []ent.Value {
	switch name {
	case listen.EdgeUser:
		if id := m.user; id != nil {
			return []ent.Value{*id}
		}
	}
	return nil
}

// RemovedEdges returns all edge names that were removed in this mutation.
func (m *ListenMutation) RemovedEdges() []string {
	edges := make([]string, 0, 1)
	return edges
}

// RemovedIDs returns all IDs (to other nodes) that were removed for the edge with
// the given name in this mutation.
func (m *ListenMutation) RemovedIDs(name string) []ent.Value {
	return nil
}

// ClearedEdges returns all edge names that were cleared in this mutation.
func (m *ListenMutation) ClearedEdges() []string {
	edges := make([]string, 0, 1)
	if m.cleareduser {
		edges = append(edges, listen.EdgeUser)
	}
	return edges
}

// EdgeCleared returns a boolean which indicates if the edge with the given name
// was cleared in this mutation.
func (m *ListenMutation) EdgeCleared(name string) bool {
	switch name {
	case listen.EdgeUser:
		return m.cleareduser
	}
	return false
}

// ClearEdge clears the value of the edge with the given name. It returns an error
// if that edge is not defined in the schema.
func (m *ListenMutation) ClearEdge(name string) error {
	switch name {
	case listen.EdgeUser:
		m.ClearUser()
		return nil
	}
	return fmt.Errorf("unknown Listen unique edge %s", name)
}

// ResetEdge resets all changes to the edge with the given name in this mutation.
// It returns an error if the edge is not defined in the schema.
func (m *ListenMutation) ResetEdge(name string) error {
	switch name {
	case listen.EdgeUser:
		m.ResetUser()
		return nil
	}
	return fmt.Errorf("unknown Listen edge %s", name)
}

// NavidromeAuthMutation represents an operation that mutates the NavidromeAuth nodes in the graph.
type NavidromeAuthMutation struct {
	config
	op            Op
	typ           string
	id            *int
	password      *string
	clearedFields map[string]struct{}
	user          *int
	cleareduser   bool
	done          bool
	oldValue      func(context.Context) (*NavidromeAuth, error)
	predicates    []predicate.NavidromeAuth
}

var _ ent.Mutation = (*NavidromeAuthMutation)(nil)

// navidromeauthOption allows management of the mutation configuration using functional options.
type navidromeauthOption func(*NavidromeAuthMutation)

// newNavidromeAuthMutation creates new mutation for the NavidromeAuth entity.
func newNavidromeAuthMutation(c config, op Op, opts ...navidromeauthOption) *NavidromeAuthMutation {
	m := &NavidromeAuthMutation{
		config:        c,
		op:            op,
		typ:           TypeNavidromeAuth,
		clearedFields: make(map[string]struct{}),
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// withNavidromeAuthID sets the ID field of the mutation.
func withNavidromeAuthID(id int) navidromeauthOption {
	return func(m *NavidromeAuthMutation) {
		var (
			err   error
			once  sync.Once
			value *NavidromeAuth
		)
		m.oldValue = func(ctx context.Context) (*NavidromeAuth, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().NavidromeAuth.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withNavidromeAuth sets the old NavidromeAuth of the mutation.
func withNavidromeAuth(node *NavidromeAuth) navidromeauthOption {
	return func(m *NavidromeAuthMutation) {
		m.oldValue = func(context.Context) (*NavidromeAuth, error) {
			return node, nil
		}
		m.id = &node.ID
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m NavidromeAuthMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m NavidromeAuthMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// ID returns the ID value in the mutation. Note that the ID is only available
// if it was provided to the builder or after it was returned from the database.
func (m *NavidromeAuthMutation) ID() (id int, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// IDs queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *NavidromeAuthMutation) IDs(ctx context.Context) ([]int, error) {
	switch {
	case m.op.Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.ID()
		if exists {
			return []int{id}, nil
		}
		fallthrough
	case m.op.Is(OpUpdate | OpDelete):
		return m.Client().NavidromeAuth.Query().Where(m.predicates...).IDs(ctx)
	default:
		return nil, fmt.Errorf("IDs is not allowed on %s operations", m.op)
	}
}

// SetPassword sets the "password" field.
func (m *NavidromeAuthMutation) SetPassword(s string) {
	m.password = &s
}

// Password returns the value of the "password" field in the mutation.
func (m *NavidromeAuthMutation) Password() (r string, exists bool) {
	v := m.password
	if v == nil {
		return
	}
	return *v, true
}

// OldPassword returns the old "password" field's value of the NavidromeAuth entity.
// If the NavidromeAuth object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *NavidromeAuthMutation) OldPassword(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldPassword is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldPassword requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldPassword: %w", err)
	}
	return oldValue.Password, nil
}

// ResetPassword resets all changes to the "password" field.
func (m *NavidromeAuthMutation) ResetPassword() {
	m.password = nil
}

// SetUserID sets the "user" edge to the User entity by id.
func (m *NavidromeAuthMutation) SetUserID(id int) {
	m.user = &id
}

// ClearUser clears the "user" edge to the User entity.
func (m *NavidromeAuthMutation) ClearUser() {
	m.cleareduser = true
}

// UserCleared reports if the "user" edge to the User entity was cleared.
func (m *NavidromeAuthMutation) UserCleared() bool {
	return m.cleareduser
}

// UserID returns the "user" edge ID in the mutation.
func (m *NavidromeAuthMutation) UserID() (id int, exists bool) {
	if m.user != nil {
		return *m.user, true
	}
	return
}

// UserIDs returns the "user" edge IDs in the mutation.
// Note that IDs always returns len(IDs) <= 1 for unique edges, and you should use
// UserID instead. It exists only for internal usage by the builders.
func (m *NavidromeAuthMutation) UserIDs() (ids []int) {
	if id := m.user; id != nil {
		ids = append(ids, *id)
	}
	return
}

// ResetUser resets all changes to the "user" edge.
func (m *NavidromeAuthMutation) ResetUser() {
	m.user = nil
	m.cleareduser = false
}

// Where appends a list predicates to the NavidromeAuthMutation builder.
func (m *NavidromeAuthMutation) Where(ps ...predicate.NavidromeAuth) {
	m.predicates = append(m.predicates, ps...)
}

// WhereP appends storage-level predicates to the NavidromeAuthMutation builder. Using this method,
// users can use type-assertion to append predicates that do not depend on any generated package.
func (m *NavidromeAuthMutation) WhereP(ps ...func(*sql.Selector)) {
	p := make([]predicate.NavidromeAuth, len(ps))
	for i := range ps {
		p[i] = ps[i]
	}
	m.Where(p...)
}

// Op returns the operation name.
func (m *NavidromeAuthMutation) Op() Op {
	return m.op
}

// SetOp allows setting the mutation operation.
func (m *NavidromeAuthMutation) SetOp(op Op) {
	m.op = op
}

// Type returns the node type of this mutation (NavidromeAuth).
func (m *NavidromeAuthMutation) Type() string {
	return m.typ
}

// Fields returns all fields that were changed during this mutation. Note that in
// order to get all numeric fields that were incremented/decremented, call
// AddedFields().
func (m *NavidromeAuthMutation) Fields() []string {
	fields := make([]string, 0, 1)
	if m.password != nil {
		fields = append(fields, navidromeauth.FieldPassword)
	}
	return fields
}

// Field returns the value of a field with the given name. The second boolean
// return value indicates that this field was not set, or was not defined in the
// schema.
func (m *NavidromeAuthMutation) Field(name string) (ent.Value, bool) {
	switch name {
	case navidromeauth.FieldPassword:
		return m.Password()
	}
	return nil, false
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *NavidromeAuthMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case navidromeauth.FieldPassword:
		return m.OldPassword(ctx)
	}
	return nil, fmt.Errorf("unknown NavidromeAuth field %s", name)
}

// SetField sets the value of a field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *NavidromeAuthMutation) SetField(name string, value ent.Value) error {
	switch name {
	case navidromeauth.FieldPassword:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetPassword(v)
		return nil
	}
	return fmt.Errorf("unknown NavidromeAuth field %s", name)
}

// AddedFields returns all numeric fields that were incremented/decremented during
// this mutation.
func (m *NavidromeAuthMutation) AddedFields() []string {
	return nil
}

// AddedField returns the numeric value that was incremented/decremented on a field
// with the given name. The second boolean return value indicates that this field
// was not set, or was not defined in the schema.
func (m *NavidromeAuthMutation) AddedField(name string) (ent.Value, bool) {
	return nil, false
}

// AddField adds the value to the field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *NavidromeAuthMutation) AddField(name string, value ent.Value) error {
	switch name {
	}
	return fmt.Errorf("unknown NavidromeAuth numeric field %s", name)
}

// ClearedFields returns all nullable fields that were cleared during this
// mutation.
func (m *NavidromeAuthMutation) ClearedFields() []string {
	return nil
}

// FieldCleared returns a boolean indicating if a field with the given name was
// cleared in this mutation.
func (m *NavidromeAuthMutation) FieldCleared(name string) bool {
	_, ok := m.clearedFields[name]
	return ok
}

// ClearField clears the value of the field with the given name. It returns an
// error if the field is not defined in the schema.
func (m *NavidromeAuthMutation) ClearField(name string) error {
	return fmt.Errorf("unknown NavidromeAuth nullable field %s", name)
}

// ResetField resets all changes in the mutation for the field with the given name.
// It returns an error if the field is not defined in the schema.
func (m *NavidromeAuthMutation) ResetField(name string) error {
	switch name {
	case navidromeauth.FieldPassword:
		m.ResetPassword()
		return nil
	}
	return fmt.Errorf("unknown NavidromeAuth field %s", name)
}

// AddedEdges returns all edge names that were set/added in this mutation.
func (m *NavidromeAuthMutation) AddedEdges() []string {
	edges := make([]string, 0, 1)
	if m.user != nil {
		edges = append(edges, navidromeauth.EdgeUser)
	}
	return edges
}

// AddedIDs returns all IDs (to other nodes) that were added for the given edge
// name in this mutation.
func (m *NavidromeAuthMutation) AddedIDs(name string) []ent.Value {
	switch name {
	case navidromeauth.EdgeUser:
		if id := m.user; id != nil {
			return []ent.Value{*id}
		}
	}
	return nil
}

// RemovedEdges returns all edge names that were removed in this mutation.
func (m *NavidromeAuthMutation) RemovedEdges() []string {
	edges := make([]string, 0, 1)
	return edges
}

// RemovedIDs returns all IDs (to other nodes) that were removed for the edge with
// the given name in this mutation.
func (m *NavidromeAuthMutation) RemovedIDs(name string) []ent.Value {
	return nil
}

// ClearedEdges returns all edge names that were cleared in this mutation.
func (m *NavidromeAuthMutation) ClearedEdges() []string {
	edges := make([]string, 0, 1)
	if m.cleareduser {
		edges = append(edges, navidromeauth.EdgeUser)
	}
	return edges
}

// EdgeCleared returns a boolean which indicates if the edge with the given name
// was cleared in this mutation.
func (m *NavidromeAuthMutation) EdgeCleared(name string) bool {
	switch name {
	case navidromeauth.EdgeUser:
		return m.cleareduser
	}
	return false
}

// ClearEdge clears the value of the edge with the given name. It returns an error
// if that edge is not defined in the schema.
func (m *NavidromeAuthMutation) ClearEdge(name string) error {
	switch name {
	case navidromeauth.EdgeUser:
		m.ClearUser()
		return nil
	}
	return fmt.Errorf("unknown NavidromeAuth unique edge %s", name)
}

// ResetEdge resets all changes to the edge with the given name in this mutation.
// It returns an error if the edge is not defined in the schema.
func (m *NavidromeAuthMutation) ResetEdge(name string) error {
	switch name {
	case navidromeauth.EdgeUser:
		m.ResetUser()
		return nil
	}
	return fmt.Errorf("unknown NavidromeAuth edge %s", name)
}

// SpotifyAuthMutation represents an operation that mutates the SpotifyAuth nodes in the graph.
type SpotifyAuthMutation struct {
	config
	op            Op
	typ           string
	id            *int
	access_token  *string
	refresh_token *string
	expiry        *time.Time
	clearedFields map[string]struct{}
	user          *int
	cleareduser   bool
	done          bool
	oldValue      func(context.Context) (*SpotifyAuth, error)
	predicates    []predicate.SpotifyAuth
}

var _ ent.Mutation = (*SpotifyAuthMutation)(nil)

// spotifyauthOption allows management of the mutation configuration using functional options.
type spotifyauthOption func(*SpotifyAuthMutation)

// newSpotifyAuthMutation creates new mutation for the SpotifyAuth entity.
func newSpotifyAuthMutation(c config, op Op, opts ...spotifyauthOption) *SpotifyAuthMutation {
	m := &SpotifyAuthMutation{
		config:        c,
		op:            op,
		typ:           TypeSpotifyAuth,
		clearedFields: make(map[string]struct{}),
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// withSpotifyAuthID sets the ID field of the mutation.
func withSpotifyAuthID(id int) spotifyauthOption {
	return func(m *SpotifyAuthMutation) {
		var (
			err   error
			once  sync.Once
			value *SpotifyAuth
		)
		m.oldValue = func(ctx context.Context) (*SpotifyAuth, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().SpotifyAuth.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withSpotifyAuth sets the old SpotifyAuth of the mutation.
func withSpotifyAuth(node *SpotifyAuth) spotifyauthOption {
	return func(m *SpotifyAuthMutation) {
		m.oldValue = func(context.Context) (*SpotifyAuth, error) {
			return node, nil
		}
		m.id = &node.ID
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m SpotifyAuthMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m SpotifyAuthMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// ID returns the ID value in the mutation. Note that the ID is only available
// if it was provided to the builder or after it was returned from the database.
func (m *SpotifyAuthMutation) ID() (id int, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// IDs queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *SpotifyAuthMutation) IDs(ctx context.Context) ([]int, error) {
	switch {
	case m.op.Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.ID()
		if exists {
			return []int{id}, nil
		}
		fallthrough
	case m.op.Is(OpUpdate | OpDelete):
		return m.Client().SpotifyAuth.Query().Where(m.predicates...).IDs(ctx)
	default:
		return nil, fmt.Errorf("IDs is not allowed on %s operations", m.op)
	}
}

// SetAccessToken sets the "access_token" field.
func (m *SpotifyAuthMutation) SetAccessToken(s string) {
	m.access_token = &s
}

// AccessToken returns the value of the "access_token" field in the mutation.
func (m *SpotifyAuthMutation) AccessToken() (r string, exists bool) {
	v := m.access_token
	if v == nil {
		return
	}
	return *v, true
}

// OldAccessToken returns the old "access_token" field's value of the SpotifyAuth entity.
// If the SpotifyAuth object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *SpotifyAuthMutation) OldAccessToken(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldAccessToken is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldAccessToken requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldAccessToken: %w", err)
	}
	return oldValue.AccessToken, nil
}

// ResetAccessToken resets all changes to the "access_token" field.
func (m *SpotifyAuthMutation) ResetAccessToken() {
	m.access_token = nil
}

// SetRefreshToken sets the "refresh_token" field.
func (m *SpotifyAuthMutation) SetRefreshToken(s string) {
	m.refresh_token = &s
}

// RefreshToken returns the value of the "refresh_token" field in the mutation.
func (m *SpotifyAuthMutation) RefreshToken() (r string, exists bool) {
	v := m.refresh_token
	if v == nil {
		return
	}
	return *v, true
}

// OldRefreshToken returns the old "refresh_token" field's value of the SpotifyAuth entity.
// If the SpotifyAuth object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *SpotifyAuthMutation) OldRefreshToken(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldRefreshToken is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldRefreshToken requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldRefreshToken: %w", err)
	}
	return oldValue.RefreshToken, nil
}

// ResetRefreshToken resets all changes to the "refresh_token" field.
func (m *SpotifyAuthMutation) ResetRefreshToken() {
	m.refresh_token = nil
}

// SetExpiry sets the "expiry" field.
func (m *SpotifyAuthMutation) SetExpiry(t time.Time) {
	m.expiry = &t
}

// Expiry returns the value of the "expiry" field in the mutation.
func (m *SpotifyAuthMutation) Expiry() (r time.Time, exists bool) {
	v := m.expiry
	if v == nil {
		return
	}
	return *v, true
}

// OldExpiry returns the old "expiry" field's value of the SpotifyAuth entity.
// If the SpotifyAuth object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *SpotifyAuthMutation) OldExpiry(ctx context.Context) (v time.Time, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldExpiry is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldExpiry requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldExpiry: %w", err)
	}
	return oldValue.Expiry, nil
}

// ResetExpiry resets all changes to the "expiry" field.
func (m *SpotifyAuthMutation) ResetExpiry() {
	m.expiry = nil
}

// SetUserID sets the "user" edge to the User entity by id.
func (m *SpotifyAuthMutation) SetUserID(id int) {
	m.user = &id
}

// ClearUser clears the "user" edge to the User entity.
func (m *SpotifyAuthMutation) ClearUser() {
	m.cleareduser = true
}

// UserCleared reports if the "user" edge to the User entity was cleared.
func (m *SpotifyAuthMutation) UserCleared() bool {
	return m.cleareduser
}

// UserID returns the "user" edge ID in the mutation.
func (m *SpotifyAuthMutation) UserID() (id int, exists bool) {
	if m.user != nil {
		return *m.user, true
	}
	return
}

// UserIDs returns the "user" edge IDs in the mutation.
// Note that IDs always returns len(IDs) <= 1 for unique edges, and you should use
// UserID instead. It exists only for internal usage by the builders.
func (m *SpotifyAuthMutation) UserIDs() (ids []int) {
	if id := m.user; id != nil {
		ids = append(ids, *id)
	}
	return
}

// ResetUser resets all changes to the "user" edge.
func (m *SpotifyAuthMutation) ResetUser() {
	m.user = nil
	m.cleareduser = false
}

// Where appends a list predicates to the SpotifyAuthMutation builder.
func (m *SpotifyAuthMutation) Where(ps ...predicate.SpotifyAuth) {
	m.predicates = append(m.predicates, ps...)
}

// WhereP appends storage-level predicates to the SpotifyAuthMutation builder. Using this method,
// users can use type-assertion to append predicates that do not depend on any generated package.
func (m *SpotifyAuthMutation) WhereP(ps ...func(*sql.Selector)) {
	p := make([]predicate.SpotifyAuth, len(ps))
	for i := range ps {
		p[i] = ps[i]
	}
	m.Where(p...)
}

// Op returns the operation name.
func (m *SpotifyAuthMutation) Op() Op {
	return m.op
}

// SetOp allows setting the mutation operation.
func (m *SpotifyAuthMutation) SetOp(op Op) {
	m.op = op
}

// Type returns the node type of this mutation (SpotifyAuth).
func (m *SpotifyAuthMutation) Type() string {
	return m.typ
}

// Fields returns all fields that were changed during this mutation. Note that in
// order to get all numeric fields that were incremented/decremented, call
// AddedFields().
func (m *SpotifyAuthMutation) Fields() []string {
	fields := make([]string, 0, 3)
	if m.access_token != nil {
		fields = append(fields, spotifyauth.FieldAccessToken)
	}
	if m.refresh_token != nil {
		fields = append(fields, spotifyauth.FieldRefreshToken)
	}
	if m.expiry != nil {
		fields = append(fields, spotifyauth.FieldExpiry)
	}
	return fields
}

// Field returns the value of a field with the given name. The second boolean
// return value indicates that this field was not set, or was not defined in the
// schema.
func (m *SpotifyAuthMutation) Field(name string) (ent.Value, bool) {
	switch name {
	case spotifyauth.FieldAccessToken:
		return m.AccessToken()
	case spotifyauth.FieldRefreshToken:
		return m.RefreshToken()
	case spotifyauth.FieldExpiry:
		return m.Expiry()
	}
	return nil, false
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *SpotifyAuthMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case spotifyauth.FieldAccessToken:
		return m.OldAccessToken(ctx)
	case spotifyauth.FieldRefreshToken:
		return m.OldRefreshToken(ctx)
	case spotifyauth.FieldExpiry:
		return m.OldExpiry(ctx)
	}
	return nil, fmt.Errorf("unknown SpotifyAuth field %s", name)
}

// SetField sets the value of a field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *SpotifyAuthMutation) SetField(name string, value ent.Value) error {
	switch name {
	case spotifyauth.FieldAccessToken:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetAccessToken(v)
		return nil
	case spotifyauth.FieldRefreshToken:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetRefreshToken(v)
		return nil
	case spotifyauth.FieldExpiry:
		v, ok := value.(time.Time)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetExpiry(v)
		return nil
	}
	return fmt.Errorf("unknown SpotifyAuth field %s", name)
}

// AddedFields returns all numeric fields that were incremented/decremented during
// this mutation.
func (m *SpotifyAuthMutation) AddedFields() []string {
	return nil
}

// AddedField returns the numeric value that was incremented/decremented on a field
// with the given name. The second boolean return value indicates that this field
// was not set, or was not defined in the schema.
func (m *SpotifyAuthMutation) AddedField(name string) (ent.Value, bool) {
	return nil, false
}

// AddField adds the value to the field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *SpotifyAuthMutation) AddField(name string, value ent.Value) error {
	switch name {
	}
	return fmt.Errorf("unknown SpotifyAuth numeric field %s", name)
}

// ClearedFields returns all nullable fields that were cleared during this
// mutation.
func (m *SpotifyAuthMutation) ClearedFields() []string {
	return nil
}

// FieldCleared returns a boolean indicating if a field with the given name was
// cleared in this mutation.
func (m *SpotifyAuthMutation) FieldCleared(name string) bool {
	_, ok := m.clearedFields[name]
	return ok
}

// ClearField clears the value of the field with the given name. It returns an
// error if the field is not defined in the schema.
func (m *SpotifyAuthMutation) ClearField(name string) error {
	return fmt.Errorf("unknown SpotifyAuth nullable field %s", name)
}

// ResetField resets all changes in the mutation for the field with the given name.
// It returns an error if the field is not defined in the schema.
func (m *SpotifyAuthMutation) ResetField(name string) error {
	switch name {
	case spotifyauth.FieldAccessToken:
		m.ResetAccessToken()
		return nil
	case spotifyauth.FieldRefreshToken:
		m.ResetRefreshToken()
		return nil
	case spotifyauth.FieldExpiry:
		m.ResetExpiry()
		return nil
	}
	return fmt.Errorf("unknown SpotifyAuth field %s", name)
}

// AddedEdges returns all edge names that were set/added in this mutation.
func (m *SpotifyAuthMutation) AddedEdges() []string {
	edges := make([]string, 0, 1)
	if m.user != nil {
		edges = append(edges, spotifyauth.EdgeUser)
	}
	return edges
}

// AddedIDs returns all IDs (to other nodes) that were added for the given edge
// name in this mutation.
func (m *SpotifyAuthMutation) AddedIDs(name string) []ent.Value {
	switch name {
	case spotifyauth.EdgeUser:
		if id := m.user; id != nil {
			return []ent.Value{*id}
		}
	}
	return nil
}

// RemovedEdges returns all edge names that were removed in this mutation.
func (m *SpotifyAuthMutation) RemovedEdges() []string {
	edges := make([]string, 0, 1)
	return edges
}

// RemovedIDs returns all IDs (to other nodes) that were removed for the edge with
// the given name in this mutation.
func (m *SpotifyAuthMutation) RemovedIDs(name string) []ent.Value {
	return nil
}

// ClearedEdges returns all edge names that were cleared in this mutation.
func (m *SpotifyAuthMutation) ClearedEdges() []string {
	edges := make([]string, 0, 1)
	if m.cleareduser {
		edges = append(edges, spotifyauth.EdgeUser)
	}
	return edges
}

// EdgeCleared returns a boolean which indicates if the edge with the given name
// was cleared in this mutation.
func (m *SpotifyAuthMutation) EdgeCleared(name string) bool {
	switch name {
	case spotifyauth.EdgeUser:
		return m.cleareduser
	}
	return false
}

// ClearEdge clears the value of the edge with the given name. It returns an error
// if that edge is not defined in the schema.
func (m *SpotifyAuthMutation) ClearEdge(name string) error {
	switch name {
	case spotifyauth.EdgeUser:
		m.ClearUser()
		return nil
	}
	return fmt.Errorf("unknown SpotifyAuth unique edge %s", name)
}

// ResetEdge resets all changes to the edge with the given name in this mutation.
// It returns an error if the edge is not defined in the schema.
func (m *SpotifyAuthMutation) ResetEdge(name string) error {
	switch name {
	case spotifyauth.EdgeUser:
		m.ResetUser()
		return nil
	}
	return fmt.Errorf("unknown SpotifyAuth edge %s", name)
}

// UserMutation represents an operation that mutates the User nodes in the graph.
type UserMutation struct {
	config
	op                    Op
	typ                   string
	id                    *int
	username              *string
	email                 *string
	last_login_at         *time.Time
	clearedFields         map[string]struct{}
	spotify_auth          *int
	clearedspotify_auth   bool
	lastfm_auth           *int
	clearedlastfm_auth    bool
	navidrome_auth        *int
	clearednavidrome_auth bool
	listens               map[int]struct{}
	removedlistens        map[int]struct{}
	clearedlistens        bool
	done                  bool
	oldValue              func(context.Context) (*User, error)
	predicates            []predicate.User
}

var _ ent.Mutation = (*UserMutation)(nil)

// userOption allows management of the mutation configuration using functional options.
type userOption func(*UserMutation)

// newUserMutation creates new mutation for the User entity.
func newUserMutation(c config, op Op, opts ...userOption) *UserMutation {
	m := &UserMutation{
		config:        c,
		op:            op,
		typ:           TypeUser,
		clearedFields: make(map[string]struct{}),
	}
	for _, opt := range opts {
		opt(m)
	}
	return m
}

// withUserID sets the ID field of the mutation.
func withUserID(id int) userOption {
	return func(m *UserMutation) {
		var (
			err   error
			once  sync.Once
			value *User
		)
		m.oldValue = func(ctx context.Context) (*User, error) {
			once.Do(func() {
				if m.done {
					err = errors.New("querying old values post mutation is not allowed")
				} else {
					value, err = m.Client().User.Get(ctx, id)
				}
			})
			return value, err
		}
		m.id = &id
	}
}

// withUser sets the old User of the mutation.
func withUser(node *User) userOption {
	return func(m *UserMutation) {
		m.oldValue = func(context.Context) (*User, error) {
			return node, nil
		}
		m.id = &node.ID
	}
}

// Client returns a new `ent.Client` from the mutation. If the mutation was
// executed in a transaction (ent.Tx), a transactional client is returned.
func (m UserMutation) Client() *Client {
	client := &Client{config: m.config}
	client.init()
	return client
}

// Tx returns an `ent.Tx` for mutations that were executed in transactions;
// it returns an error otherwise.
func (m UserMutation) Tx() (*Tx, error) {
	if _, ok := m.driver.(*txDriver); !ok {
		return nil, errors.New("ent: mutation is not running in a transaction")
	}
	tx := &Tx{config: m.config}
	tx.init()
	return tx, nil
}

// ID returns the ID value in the mutation. Note that the ID is only available
// if it was provided to the builder or after it was returned from the database.
func (m *UserMutation) ID() (id int, exists bool) {
	if m.id == nil {
		return
	}
	return *m.id, true
}

// IDs queries the database and returns the entity ids that match the mutation's predicate.
// That means, if the mutation is applied within a transaction with an isolation level such
// as sql.LevelSerializable, the returned ids match the ids of the rows that will be updated
// or updated by the mutation.
func (m *UserMutation) IDs(ctx context.Context) ([]int, error) {
	switch {
	case m.op.Is(OpUpdateOne | OpDeleteOne):
		id, exists := m.ID()
		if exists {
			return []int{id}, nil
		}
		fallthrough
	case m.op.Is(OpUpdate | OpDelete):
		return m.Client().User.Query().Where(m.predicates...).IDs(ctx)
	default:
		return nil, fmt.Errorf("IDs is not allowed on %s operations", m.op)
	}
}

// SetUsername sets the "username" field.
func (m *UserMutation) SetUsername(s string) {
	m.username = &s
}

// Username returns the value of the "username" field in the mutation.
func (m *UserMutation) Username() (r string, exists bool) {
	v := m.username
	if v == nil {
		return
	}
	return *v, true
}

// OldUsername returns the old "username" field's value of the User entity.
// If the User object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *UserMutation) OldUsername(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldUsername is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldUsername requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldUsername: %w", err)
	}
	return oldValue.Username, nil
}

// ResetUsername resets all changes to the "username" field.
func (m *UserMutation) ResetUsername() {
	m.username = nil
}

// SetEmail sets the "email" field.
func (m *UserMutation) SetEmail(s string) {
	m.email = &s
}

// Email returns the value of the "email" field in the mutation.
func (m *UserMutation) Email() (r string, exists bool) {
	v := m.email
	if v == nil {
		return
	}
	return *v, true
}

// OldEmail returns the old "email" field's value of the User entity.
// If the User object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *UserMutation) OldEmail(ctx context.Context) (v string, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldEmail is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldEmail requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldEmail: %w", err)
	}
	return oldValue.Email, nil
}

// ClearEmail clears the value of the "email" field.
func (m *UserMutation) ClearEmail() {
	m.email = nil
	m.clearedFields[user.FieldEmail] = struct{}{}
}

// EmailCleared returns if the "email" field was cleared in this mutation.
func (m *UserMutation) EmailCleared() bool {
	_, ok := m.clearedFields[user.FieldEmail]
	return ok
}

// ResetEmail resets all changes to the "email" field.
func (m *UserMutation) ResetEmail() {
	m.email = nil
	delete(m.clearedFields, user.FieldEmail)
}

// SetLastLoginAt sets the "last_login_at" field.
func (m *UserMutation) SetLastLoginAt(t time.Time) {
	m.last_login_at = &t
}

// LastLoginAt returns the value of the "last_login_at" field in the mutation.
func (m *UserMutation) LastLoginAt() (r time.Time, exists bool) {
	v := m.last_login_at
	if v == nil {
		return
	}
	return *v, true
}

// OldLastLoginAt returns the old "last_login_at" field's value of the User entity.
// If the User object wasn't provided to the builder, the object is fetched from the database.
// An error is returned if the mutation operation is not UpdateOne, or the database query fails.
func (m *UserMutation) OldLastLoginAt(ctx context.Context) (v time.Time, err error) {
	if !m.op.Is(OpUpdateOne) {
		return v, errors.New("OldLastLoginAt is only allowed on UpdateOne operations")
	}
	if m.id == nil || m.oldValue == nil {
		return v, errors.New("OldLastLoginAt requires an ID field in the mutation")
	}
	oldValue, err := m.oldValue(ctx)
	if err != nil {
		return v, fmt.Errorf("querying old value for OldLastLoginAt: %w", err)
	}
	return oldValue.LastLoginAt, nil
}

// ResetLastLoginAt resets all changes to the "last_login_at" field.
func (m *UserMutation) ResetLastLoginAt() {
	m.last_login_at = nil
}

// SetSpotifyAuthID sets the "spotify_auth" edge to the SpotifyAuth entity by id.
func (m *UserMutation) SetSpotifyAuthID(id int) {
	m.spotify_auth = &id
}

// ClearSpotifyAuth clears the "spotify_auth" edge to the SpotifyAuth entity.
func (m *UserMutation) ClearSpotifyAuth() {
	m.clearedspotify_auth = true
}

// SpotifyAuthCleared reports if the "spotify_auth" edge to the SpotifyAuth entity was cleared.
func (m *UserMutation) SpotifyAuthCleared() bool {
	return m.clearedspotify_auth
}

// SpotifyAuthID returns the "spotify_auth" edge ID in the mutation.
func (m *UserMutation) SpotifyAuthID() (id int, exists bool) {
	if m.spotify_auth != nil {
		return *m.spotify_auth, true
	}
	return
}

// SpotifyAuthIDs returns the "spotify_auth" edge IDs in the mutation.
// Note that IDs always returns len(IDs) <= 1 for unique edges, and you should use
// SpotifyAuthID instead. It exists only for internal usage by the builders.
func (m *UserMutation) SpotifyAuthIDs() (ids []int) {
	if id := m.spotify_auth; id != nil {
		ids = append(ids, *id)
	}
	return
}

// ResetSpotifyAuth resets all changes to the "spotify_auth" edge.
func (m *UserMutation) ResetSpotifyAuth() {
	m.spotify_auth = nil
	m.clearedspotify_auth = false
}

// SetLastfmAuthID sets the "lastfm_auth" edge to the LastFMAuth entity by id.
func (m *UserMutation) SetLastfmAuthID(id int) {
	m.lastfm_auth = &id
}

// ClearLastfmAuth clears the "lastfm_auth" edge to the LastFMAuth entity.
func (m *UserMutation) ClearLastfmAuth() {
	m.clearedlastfm_auth = true
}

// LastfmAuthCleared reports if the "lastfm_auth" edge to the LastFMAuth entity was cleared.
func (m *UserMutation) LastfmAuthCleared() bool {
	return m.clearedlastfm_auth
}

// LastfmAuthID returns the "lastfm_auth" edge ID in the mutation.
func (m *UserMutation) LastfmAuthID() (id int, exists bool) {
	if m.lastfm_auth != nil {
		return *m.lastfm_auth, true
	}
	return
}

// LastfmAuthIDs returns the "lastfm_auth" edge IDs in the mutation.
// Note that IDs always returns len(IDs) <= 1 for unique edges, and you should use
// LastfmAuthID instead. It exists only for internal usage by the builders.
func (m *UserMutation) LastfmAuthIDs() (ids []int) {
	if id := m.lastfm_auth; id != nil {
		ids = append(ids, *id)
	}
	return
}

// ResetLastfmAuth resets all changes to the "lastfm_auth" edge.
func (m *UserMutation) ResetLastfmAuth() {
	m.lastfm_auth = nil
	m.clearedlastfm_auth = false
}

// SetNavidromeAuthID sets the "navidrome_auth" edge to the NavidromeAuth entity by id.
func (m *UserMutation) SetNavidromeAuthID(id int) {
	m.navidrome_auth = &id
}

// ClearNavidromeAuth clears the "navidrome_auth" edge to the NavidromeAuth entity.
func (m *UserMutation) ClearNavidromeAuth() {
	m.clearednavidrome_auth = true
}

// NavidromeAuthCleared reports if the "navidrome_auth" edge to the NavidromeAuth entity was cleared.
func (m *UserMutation) NavidromeAuthCleared() bool {
	return m.clearednavidrome_auth
}

// NavidromeAuthID returns the "navidrome_auth" edge ID in the mutation.
func (m *UserMutation) NavidromeAuthID() (id int, exists bool) {
	if m.navidrome_auth != nil {
		return *m.navidrome_auth, true
	}
	return
}

// NavidromeAuthIDs returns the "navidrome_auth" edge IDs in the mutation.
// Note that IDs always returns len(IDs) <= 1 for unique edges, and you should use
// NavidromeAuthID instead. It exists only for internal usage by the builders.
func (m *UserMutation) NavidromeAuthIDs() (ids []int) {
	if id := m.navidrome_auth; id != nil {
		ids = append(ids, *id)
	}
	return
}

// ResetNavidromeAuth resets all changes to the "navidrome_auth" edge.
func (m *UserMutation) ResetNavidromeAuth() {
	m.navidrome_auth = nil
	m.clearednavidrome_auth = false
}

// AddListenIDs adds the "listens" edge to the Listen entity by ids.
func (m *UserMutation) AddListenIDs(ids ...int) {
	if m.listens == nil {
		m.listens = make(map[int]struct{})
	}
	for i := range ids {
		m.listens[ids[i]] = struct{}{}
	}
}

// ClearListens clears the "listens" edge to the Listen entity.
func (m *UserMutation) ClearListens() {
	m.clearedlistens = true
}

// ListensCleared reports if the "listens" edge to the Listen entity was cleared.
func (m *UserMutation) ListensCleared() bool {
	return m.clearedlistens
}

// RemoveListenIDs removes the "listens" edge to the Listen entity by IDs.
func (m *UserMutation) RemoveListenIDs(ids ...int) {
	if m.removedlistens == nil {
		m.removedlistens = make(map[int]struct{})
	}
	for i := range ids {
		delete(m.listens, ids[i])
		m.removedlistens[ids[i]] = struct{}{}
	}
}

// RemovedListens returns the removed IDs of the "listens" edge to the Listen entity.
func (m *UserMutation) RemovedListensIDs() (ids []int) {
	for id := range m.removedlistens {
		ids = append(ids, id)
	}
	return
}

// ListensIDs returns the "listens" edge IDs in the mutation.
func (m *UserMutation) ListensIDs() (ids []int) {
	for id := range m.listens {
		ids = append(ids, id)
	}
	return
}

// ResetListens resets all changes to the "listens" edge.
func (m *UserMutation) ResetListens() {
	m.listens = nil
	m.clearedlistens = false
	m.removedlistens = nil
}

// Where appends a list predicates to the UserMutation builder.
func (m *UserMutation) Where(ps ...predicate.User) {
	m.predicates = append(m.predicates, ps...)
}

// WhereP appends storage-level predicates to the UserMutation builder. Using this method,
// users can use type-assertion to append predicates that do not depend on any generated package.
func (m *UserMutation) WhereP(ps ...func(*sql.Selector)) {
	p := make([]predicate.User, len(ps))
	for i := range ps {
		p[i] = ps[i]
	}
	m.Where(p...)
}

// Op returns the operation name.
func (m *UserMutation) Op() Op {
	return m.op
}

// SetOp allows setting the mutation operation.
func (m *UserMutation) SetOp(op Op) {
	m.op = op
}

// Type returns the node type of this mutation (User).
func (m *UserMutation) Type() string {
	return m.typ
}

// Fields returns all fields that were changed during this mutation. Note that in
// order to get all numeric fields that were incremented/decremented, call
// AddedFields().
func (m *UserMutation) Fields() []string {
	fields := make([]string, 0, 3)
	if m.username != nil {
		fields = append(fields, user.FieldUsername)
	}
	if m.email != nil {
		fields = append(fields, user.FieldEmail)
	}
	if m.last_login_at != nil {
		fields = append(fields, user.FieldLastLoginAt)
	}
	return fields
}

// Field returns the value of a field with the given name. The second boolean
// return value indicates that this field was not set, or was not defined in the
// schema.
func (m *UserMutation) Field(name string) (ent.Value, bool) {
	switch name {
	case user.FieldUsername:
		return m.Username()
	case user.FieldEmail:
		return m.Email()
	case user.FieldLastLoginAt:
		return m.LastLoginAt()
	}
	return nil, false
}

// OldField returns the old value of the field from the database. An error is
// returned if the mutation operation is not UpdateOne, or the query to the
// database failed.
func (m *UserMutation) OldField(ctx context.Context, name string) (ent.Value, error) {
	switch name {
	case user.FieldUsername:
		return m.OldUsername(ctx)
	case user.FieldEmail:
		return m.OldEmail(ctx)
	case user.FieldLastLoginAt:
		return m.OldLastLoginAt(ctx)
	}
	return nil, fmt.Errorf("unknown User field %s", name)
}

// SetField sets the value of a field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *UserMutation) SetField(name string, value ent.Value) error {
	switch name {
	case user.FieldUsername:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetUsername(v)
		return nil
	case user.FieldEmail:
		v, ok := value.(string)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetEmail(v)
		return nil
	case user.FieldLastLoginAt:
		v, ok := value.(time.Time)
		if !ok {
			return fmt.Errorf("unexpected type %T for field %s", value, name)
		}
		m.SetLastLoginAt(v)
		return nil
	}
	return fmt.Errorf("unknown User field %s", name)
}

// AddedFields returns all numeric fields that were incremented/decremented during
// this mutation.
func (m *UserMutation) AddedFields() []string {
	return nil
}

// AddedField returns the numeric value that was incremented/decremented on a field
// with the given name. The second boolean return value indicates that this field
// was not set, or was not defined in the schema.
func (m *UserMutation) AddedField(name string) (ent.Value, bool) {
	return nil, false
}

// AddField adds the value to the field with the given name. It returns an error if
// the field is not defined in the schema, or if the type mismatched the field
// type.
func (m *UserMutation) AddField(name string, value ent.Value) error {
	switch name {
	}
	return fmt.Errorf("unknown User numeric field %s", name)
}

// ClearedFields returns all nullable fields that were cleared during this
// mutation.
func (m *UserMutation) ClearedFields() []string {
	var fields []string
	if m.FieldCleared(user.FieldEmail) {
		fields = append(fields, user.FieldEmail)
	}
	return fields
}

// FieldCleared returns a boolean indicating if a field with the given name was
// cleared in this mutation.
func (m *UserMutation) FieldCleared(name string) bool {
	_, ok := m.clearedFields[name]
	return ok
}

// ClearField clears the value of the field with the given name. It returns an
// error if the field is not defined in the schema.
func (m *UserMutation) ClearField(name string) error {
	switch name {
	case user.FieldEmail:
		m.ClearEmail()
		return nil
	}
	return fmt.Errorf("unknown User nullable field %s", name)
}

// ResetField resets all changes in the mutation for the field with the given name.
// It returns an error if the field is not defined in the schema.
func (m *UserMutation) ResetField(name string) error {
	switch name {
	case user.FieldUsername:
		m.ResetUsername()
		return nil
	case user.FieldEmail:
		m.ResetEmail()
		return nil
	case user.FieldLastLoginAt:
		m.ResetLastLoginAt()
		return nil
	}
	return fmt.Errorf("unknown User field %s", name)
}

// AddedEdges returns all edge names that were set/added in this mutation.
func (m *UserMutation) AddedEdges() []string {
	edges := make([]string, 0, 4)
	if m.spotify_auth != nil {
		edges = append(edges, user.EdgeSpotifyAuth)
	}
	if m.lastfm_auth != nil {
		edges = append(edges, user.EdgeLastfmAuth)
	}
	if m.navidrome_auth != nil {
		edges = append(edges, user.EdgeNavidromeAuth)
	}
	if m.listens != nil {
		edges = append(edges, user.EdgeListens)
	}
	return edges
}

// AddedIDs returns all IDs (to other nodes) that were added for the given edge
// name in this mutation.
func (m *UserMutation) AddedIDs(name string) []ent.Value {
	switch name {
	case user.EdgeSpotifyAuth:
		if id := m.spotify_auth; id != nil {
			return []ent.Value{*id}
		}
	case user.EdgeLastfmAuth:
		if id := m.lastfm_auth; id != nil {
			return []ent.Value{*id}
		}
	case user.EdgeNavidromeAuth:
		if id := m.navidrome_auth; id != nil {
			return []ent.Value{*id}
		}
	case user.EdgeListens:
		ids := make([]ent.Value, 0, len(m.listens))
		for id := range m.listens {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// RemovedEdges returns all edge names that were removed in this mutation.
func (m *UserMutation) RemovedEdges() []string {
	edges := make([]string, 0, 4)
	if m.removedlistens != nil {
		edges = append(edges, user.EdgeListens)
	}
	return edges
}

// RemovedIDs returns all IDs (to other nodes) that were removed for the edge with
// the given name in this mutation.
func (m *UserMutation) RemovedIDs(name string) []ent.Value {
	switch name {
	case user.EdgeListens:
		ids := make([]ent.Value, 0, len(m.removedlistens))
		for id := range m.removedlistens {
			ids = append(ids, id)
		}
		return ids
	}
	return nil
}

// ClearedEdges returns all edge names that were cleared in this mutation.
func (m *UserMutation) ClearedEdges() []string {
	edges := make([]string, 0, 4)
	if m.clearedspotify_auth {
		edges = append(edges, user.EdgeSpotifyAuth)
	}
	if m.clearedlastfm_auth {
		edges = append(edges, user.EdgeLastfmAuth)
	}
	if m.clearednavidrome_auth {
		edges = append(edges, user.EdgeNavidromeAuth)
	}
	if m.clearedlistens {
		edges = append(edges, user.EdgeListens)
	}
	return edges
}

// EdgeCleared returns a boolean which indicates if the edge with the given name
// was cleared in this mutation.
func (m *UserMutation) EdgeCleared(name string) bool {
	switch name {
	case user.EdgeSpotifyAuth:
		return m.clearedspotify_auth
	case user.EdgeLastfmAuth:
		return m.clearedlastfm_auth
	case user.EdgeNavidromeAuth:
		return m.clearednavidrome_auth
	case user.EdgeListens:
		return m.clearedlistens
	}
	return false
}

// ClearEdge clears the value of the edge with the given name. It returns an error
// if that edge is not defined in the schema.
func (m *UserMutation) ClearEdge(name string) error {
	switch name {
	case user.EdgeSpotifyAuth:
		m.ClearSpotifyAuth()
		return nil
	case user.EdgeLastfmAuth:
		m.ClearLastfmAuth()
		return nil
	case user.EdgeNavidromeAuth:
		m.ClearNavidromeAuth()
		return nil
	}
	return fmt.Errorf("unknown User unique edge %s", name)
}

// ResetEdge resets all changes to the edge with the given name in this mutation.
// It returns an error if the edge is not defined in the schema.
func (m *UserMutation) ResetEdge(name string) error {
	switch name {
	case user.EdgeSpotifyAuth:
		m.ResetSpotifyAuth()
		return nil
	case user.EdgeLastfmAuth:
		m.ResetLastfmAuth()
		return nil
	case user.EdgeNavidromeAuth:
		m.ResetNavidromeAuth()
		return nil
	case user.EdgeListens:
		m.ResetListens()
		return nil
	}
	return fmt.Errorf("unknown User edge %s", name)
}
