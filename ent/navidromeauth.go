// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"spotter/ent/navidromeauth"
	"spotter/ent/user"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// NavidromeAuth is the model entity for the NavidromeAuth schema.
type NavidromeAuth struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Password holds the value of the "password" field.
	Password string `json:"password,omitempty"`
	// LastSyncedAt holds the value of the "last_synced_at" field.
	LastSyncedAt time.Time `json:"last_synced_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the NavidromeAuthQuery when eager-loading is set.
	Edges               NavidromeAuthEdges `json:"edges"`
	user_navidrome_auth *int
	selectValues        sql.SelectValues
}

// NavidromeAuthEdges holds the relations/edges for other nodes in the graph.
type NavidromeAuthEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e NavidromeAuthEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*NavidromeAuth) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case navidromeauth.FieldID:
			values[i] = new(sql.NullInt64)
		case navidromeauth.FieldPassword:
			values[i] = new(sql.NullString)
		case navidromeauth.FieldLastSyncedAt:
			values[i] = new(sql.NullTime)
		case navidromeauth.ForeignKeys[0]: // user_navidrome_auth
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the NavidromeAuth fields.
func (_m *NavidromeAuth) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case navidromeauth.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case navidromeauth.FieldPassword:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field password", values[i])
			} else if value.Valid {
				_m.Password = value.String
			}
		case navidromeauth.FieldLastSyncedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_synced_at", values[i])
			} else if value.Valid {
				_m.LastSyncedAt = value.Time
			}
		case navidromeauth.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_navidrome_auth", value)
			} else if value.Valid {
				_m.user_navidrome_auth = new(int)
				*_m.user_navidrome_auth = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the NavidromeAuth.
// This includes values selected through modifiers, order, etc.
func (_m *NavidromeAuth) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the NavidromeAuth entity.
func (_m *NavidromeAuth) QueryUser() *UserQuery {
	return NewNavidromeAuthClient(_m.config).QueryUser(_m)
}

// Update returns a builder for updating this NavidromeAuth.
// Note that you need to call NavidromeAuth.Unwrap() before calling this method if this NavidromeAuth
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *NavidromeAuth) Update() *NavidromeAuthUpdateOne {
	return NewNavidromeAuthClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the NavidromeAuth entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *NavidromeAuth) Unwrap() *NavidromeAuth {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: NavidromeAuth is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *NavidromeAuth) String() string {
	var builder strings.Builder
	builder.WriteString("NavidromeAuth(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("password=")
	builder.WriteString(_m.Password)
	builder.WriteString(", ")
	builder.WriteString("last_synced_at=")
	builder.WriteString(_m.LastSyncedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// NavidromeAuths is a parsable slice of NavidromeAuth.
type NavidromeAuths []*NavidromeAuth
